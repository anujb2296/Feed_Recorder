# ─────────────────────────────────────────────────────────────────
#  go2rtc Configuration
#  This file is AUTO-GENERATED by the backend on startup from config.yaml.
#  Do not edit manually — your changes will be overwritten.
#  To change camera URLs, edit .env or config.yaml instead.
# ─────────────────────────────────────────────────────────────────

# Streams are populated programmatically by the backend at startup.
# This placeholder ensures go2rtc starts cleanly even before the
# backend has written the real config.
streams: {}

webrtc:
  # No STUN/TURN needed — LAN-only deployment.
  # ICE candidates use link-local addresses automatically.
  ice_servers: []

log:
  level: info
